# __init__.py

# This file allows the "app" directory to be treated as a module in Python.

# Import the main modules of the app package
default_imports = [
    "main",
    "preprocessing",
    "visualization",
    "clustering",
    "utils"
]

# Optional: Specific functions can be imported if necessary.
# from .preprocessing import clean_data
# from .visualization import plot_logs
# from .clustering import apply_kmeans
